import React from 'react';
import { NavBar } from './components';
import './styles.css';

export const Header = () => {
    return (
        <header className="header">
            <NavBar/>
        </header>
    );
};